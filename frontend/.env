# frontend/.env
# Backend API URL (without /api prefix since axios will add it via routes)
VITE_API_BASE_URL=http://localhost:3000/api/
VITE_APP_NAME=SpaceSpot
VITE_APP_VERSION=1.0.0

# Optional: Enable debug logging in development
VITE_DEBUG_API=true
